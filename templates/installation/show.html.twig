{% extends 'base.html.twig' %}

{% block title %}Installation{% endblock %}

{% block body %}
    <h1>Installation</h1>

    <table class="table">
        <tbody>
            <tr>
            </tr>
            <tr>
                <th>Montant</th>
                {% for installation in installations %}
                <td>{{ installation.montant }}</td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('installation_index') }}">back to list</a>

    <a href="{{ path('installation_edit', {'id': installation.id}) }}">edit</a>

    {{ include('installation/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('observatoire3_delete', {'id': observatoire3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ observatoire3.id) }}">
    <button class="btn">Delete</button>
</form>

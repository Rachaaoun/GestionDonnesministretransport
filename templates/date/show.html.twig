{% extends 'base.html.twig' %}

{% block title %}Date{% endblock %}

{% block body %}
    <h1>Date</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ date.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ date.annee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('date_index') }}">back to list</a>

    <a href="{{ path('date_edit', {'id': date.id}) }}">edit</a>

    {{ include('date/_delete_form.html.twig') }}
{% endblock %}

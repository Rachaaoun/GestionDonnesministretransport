<form method="post" action="{{ path('maquette2_delete', {'id': maquette2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maquette2.id) }}">
    <button class="btn">Delete</button>
</form>

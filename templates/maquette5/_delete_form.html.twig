<form method="post" action="{{ path('maquette5_delete', {'id': maquette5.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maquette5.id) }}">
    <button class="btn">Delete</button>
</form>

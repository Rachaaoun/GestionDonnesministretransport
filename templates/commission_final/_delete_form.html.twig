<form method="post" action="{{ path('commission_final_delete', {'id': commission_final.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commission_final.id) }}">
    <button class="btn">Delete</button>
</form>

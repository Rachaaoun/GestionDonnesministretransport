<form method="post" action="{{ path('tab_ca_simple_delete', {'id': tab_ca_simple.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tab_ca_simple.id) }}">
    <button class="btn">Delete</button>
</form>

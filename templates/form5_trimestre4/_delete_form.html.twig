<form method="post" action="{{ path('form5_trimestre4_delete', {'id': form5_trimestre4.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form5_trimestre4.id) }}">
    <button class="btn">Delete</button>
</form>

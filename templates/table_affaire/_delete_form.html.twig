<form method="post" action="{{ path('table_affaire_delete', {'id': table_affaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table_affaire.id) }}">
    <button class="btn">Delete</button>
</form>
